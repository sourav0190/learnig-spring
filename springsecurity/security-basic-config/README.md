# Basic Security Configuration Module

This module demonstrates the most basic Spring Security configuration using default settings and a simple filter chain.

## Features
- All requests authenticated
- Basic Authentication enabled
- Form Login enabled
- CSRF protection disabled (for development)
