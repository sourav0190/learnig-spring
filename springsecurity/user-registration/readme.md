# User Registration Module

This module handles user signup and basic employee management functionality.
It is a Spring Boot application secured with Spring Security.

## Features
- User registration (`/signup`)
- Employee data retrieval (`/emp`)
- Basic home page (`/home`)
- CSRF token endpoint (`/csrf`)
